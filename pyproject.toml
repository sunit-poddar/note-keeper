[project]
name = "note-keeper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<6.0",
    "python-slugify>=8.0",
    "gunicorn>=22.0",
    "whitenoise>=6.7",
    "Pillow>=10.0",
]
